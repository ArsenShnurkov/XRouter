<area name="Support">
  <namespaces>
    <ns>Area2</ns>
  </namespaces>
  <routes>

    <ignore pattern="{resource}.axd/{*pathInfo}" />

    <map name="default" pattern="{controller}/{action}" controller="blog" action="index" />

  </routes>
</area>
